// That's the example of how veil settings in different cases. 
// Different settings for developer environment and for production environment.
{
  "mailSettings": {
    "mailToAdress": "adminitrator@example.com"
  }
}
